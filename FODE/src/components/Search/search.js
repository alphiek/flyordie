/*import React from 'react'
import { graphql } from 'gatsby'

class Search extends React Component {
  constructor(props) {
    super(props);
    this.state = {
      sortByOption: '',
    };
    this.handleSearch = this.handleSearch.bind(this);

    this.sortByOptions = {
        {this.}
  };




    render() {
      return (
        <SearchOptionsWrapper>
          <ul>
          {this.renderSortByOptions()}
         </ul>
       </SearchOptionsWrapper>
      )
    }

}


export const query = graphql`
  query ProductFilter($sortByOptions: String!) {
    allDatoCmsProduct(range: { eq: $sortByOptions} || itemtype: { eq: $sortByOptions }) {
      range
      itemtype
    }
  }
`*/
